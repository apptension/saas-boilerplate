To simplify imports, it is recommended to use barrel exports. You can create an `index.ts` file in the desired directory
and use it to export all the components in that directory. This way, you can import multiple components from the same
directory using a single import statement.