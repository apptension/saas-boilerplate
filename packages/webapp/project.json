{
  "name": "webapp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/webapp/src",
  "projectType": "application",
  "implicitDependencies": ["tools"],
  "targets": {
    "setup": {
      "executor": "@sb/tools:setup",
      "options": {
        "cwd": "packages/webapp"
      }
    },
    "graphql": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/webapp",
        "color": true
      },
      "configurations": {
        "generate-types": {
          "commands": ["nx run webapp-api-client:graphql:generate-types"],
          "parallel": false
        },
        "generate-types:watch": {
          "commands": ["nx run webapp-api-client:graphql:generate-types:watch"]
        },
        "download-schema": {
          "commands": [
            "nx run webapp-api-client:graphql:download-schema",
            "nx run webapp-contentful:graphql:download-schema"
          ]
        }
      }
    },
    "start:app": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/webapp",
        "color": true,
        "commands": ["pnpm run vite", "nx run webapp:graphql:generate-types:watch"]
      },
      "dependsOn": ["setup", "graphql:generate-types"]
    },
    "vite-build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/webapp",
        "color": true,
        "command": "pnpm run build"
      },
      "dependsOn": ["graphql:generate-types"]
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/webapp",
        "color": true,
        "command": "nx run tools:chamber-exec webapp nx run webapp:vite-build:app"
      },
      "dependsOn": ["graphql:generate-types"]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/webapp/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/webapp",
        "color": true,
        "commands": [
          "pnpm run cdk deploy *WebAppStack",
          "nx run tools:upload-service-version webapp \"url=https://${DOMAIN_WEB_APP}\""
        ],
        "parallel": false
      }
    }
  },
  "tags": []
}
