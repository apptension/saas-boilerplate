import { Story } from '@storybook/react';
import { NotificationTypes } from '../../notifications.types';
import { {{ pascalCase name }}, {{ pascalCase name }}Props } from './{{ camelCase name }}.component';

const Template: Story<{{ pascalCase name }}Props> = (args) => {
  return <{{ pascalCase name }} {...args} />;
};

export default {
  title: 'Notifications/{{ pascalCase name }}',
  component: {{ pascalCase name }},
};

export const Default = Template.bind({});
Default.args = {
  type: NotificationTypes.{{ constantCase name }},
  readAt: null,
  createdAt: '2021-06-17T11:45:33',
  id: 'mock-uuid',
  data: {},
};
