import { gql } from '@sb/webapp-api-client/graphql';

/**
 * Mutation to submit {{ pascalCase name }} form data
 * TODO: Update this to match your backend schema
 */
export const {{ camelCase name }}FormMutation = gql(/* GraphQL */ `
  mutation {{ pascalCase name }}FormMutation($input: {{ pascalCase name }}Input!) {
    {{ camelCase name }}(input: $input) {
      {{ camelCase name }} {
        id
        # Add your response fields here
      }
    }
  }
`);
