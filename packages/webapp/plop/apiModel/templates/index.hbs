import { client } from '{{ clientPath }}';
import { apiURLs, appendId, ExtractURLParams } from '{{ helpersPath }}';
import { {{ pascalCase name }}ApiGetResponseData } from './types';
//<-- IMPORT MODULE API -->

export const {{ constantCase name }}_URL = apiURLs('/{{ camelCase name }}', {
  GET: appendId,
  LIST: '',
});

export const get = async (params: ExtractURLParams<typeof {{ constantCase name }}_URL.GET>) => {
  const res = await client.get<{{ pascalCase name }}ApiGetResponseData>({{ constantCase name }}_URL.GET(params));
  return res.data;
};

export const list = async () => {
  const res = await client.get<{{ pascalCase name }}ApiGetResponseData[]>({{ constantCase name }}_URL.LIST);
  return res.data;
};
