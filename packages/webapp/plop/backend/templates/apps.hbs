from django.apps import AppConfig


class {{ pascalCase name }}Config(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.{{ snakeCase name }}'
    verbose_name = '{{ pascalCase name }}'
