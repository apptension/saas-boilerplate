import { createSelector } from '@reduxjs/toolkit';
import { GlobalState } from '../../app/config/reducers';

export const select{{ pascalCase name }}Domain = (state: GlobalState) => state.{{ camelCase name }};

export const select{{ pascalCase name }} = createSelector(select{{ pascalCase name }}Domain, (state) => state.{{ camelCase name }});
