import { rest } from 'msw';
import { {{ pascalCase name }}ApiGetResponseData } from '../../../shared/services/api/{{ camelCase name }}/types';
import { {{ constantCase name }}_URL } from '../../../shared/services/api/{{ camelCase name }}';

export const mockGet{{ pascalCase name }} = (response: {{ pascalCase name }}ApiGetResponseData[] = []) =>
  rest.get<void, {{ pascalCase name }}ApiGetResponseData[]>({{ constantCase name }}_URL.LIST, (req, res, ctx) => {
    return res(ctx.json(response));
  });
