import { gql } from '@sb/webapp-api-client/graphql';

export const {{ camelCase name }}DetailsQuery = gql(/* GraphQL */ `
  query {{ pascalCase name }}DetailsQuery($id: ID!, $tenantId: ID!) {
    {{ camelCase name }}(id: $id, tenantId: $tenantId) {
      id
      name
    }
  }
`);
