import type { Meta, StoryObj } from '@storybook/react';

import { Edit{{ pascalCase name }} } from './edit{{ pascalCase name }}.component';

const meta: Meta<typeof Edit{{ pascalCase name }}> = {
  title: 'Routes/{{ pascalCase name }}/Edit{{ pascalCase name }}',
  component: Edit{{ pascalCase name }},
};

export default meta;

type Story = StoryObj<typeof Edit{{ pascalCase name }}>;

export const Default: Story = {};
