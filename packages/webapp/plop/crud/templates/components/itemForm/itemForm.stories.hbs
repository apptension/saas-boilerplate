import { Story } from '@storybook/react';
import { {{ camelCase name }}Factory } from '../../../mocks/factories';
import { ProvidersWrapper } from '../../../shared/utils/testUtils';
import { {{ pascalCase name }}Form, {{ pascalCase name }}FormProps } from './{{ camelCase name }}Form.component';

const item = {{ camelCase name }}Factory();

const Template: Story<{{ pascalCase name }}FormProps> = (args) => {
  return (
    <ProvidersWrapper>
      <{{ pascalCase name }}Form {...args} />
    </ProvidersWrapper>
  );
};

export default {
  title: '{{ pascalCase name }} / {{ pascalCase name }}Form',
  component: {{ pascalCase name }}Form,
};

export const WithInitialData = Template.bind({});
WithInitialData.args = { data: item };

export const WithoutData = Template.bind({});
WithoutData.args = {};
