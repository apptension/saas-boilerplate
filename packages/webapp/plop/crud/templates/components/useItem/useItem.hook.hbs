import { propEq } from 'ramda';

import { {{ pascalCase name }} } from '../../../shared/services/api/{{ camelCase name }}/types';

export const use{{ pascalCase name }} = (id: string) => {
  const allItems: {{ pascalCase name }}[] = [];
  const itemData = allItems.find(propEq('id', id));

  return itemData;
};
