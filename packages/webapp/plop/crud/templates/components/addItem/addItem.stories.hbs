import type { Meta, StoryObj } from '@storybook/react';

import { Add{{ pascalCase name }} } from './add{{ pascalCase name }}.component';

const meta: Meta<typeof Add{{ pascalCase name }}> = {
  title: 'Routes/{{ pascalCase name }}/Add{{ pascalCase name }}',
  component: Add{{ pascalCase name }},
};

export default meta;

type Story = StoryObj<typeof Add{{ pascalCase name }}>;

export const Default: Story = {};
