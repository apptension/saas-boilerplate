import { gql } from '@sb/webapp-api-client/graphql';

/**
 * Query for {{ pascalCase name }} page data
 * TODO: Update this query to match your backend schema
 */
export const {{ camelCase name }}Query = gql(/* GraphQL */ `
  query {{ pascalCase name }}Query($tenantId: ID!) {
    # TODO: Replace with your actual query
    # Example:
    # {{ camelCase name }}Stats(tenantId: $tenantId) {
    #   totalCount
    #   activeCount
    # }
    __typename
  }
`);
