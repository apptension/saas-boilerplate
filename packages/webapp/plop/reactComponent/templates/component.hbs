{{#if hasProps}}
import { ReactNode } from 'react';
{{/if}}
import { FormattedMessage } from 'react-intl';

import { Container } from './{{ camelCase name }}.styles';
{{#if hasProps}}

export type {{ pascalCase name }}Props = {
  children?: ReactNode;
}
{{/if}}

export const {{ pascalCase name }} = ({{#if hasProps}}{ children }: {{ pascalCase name }}Props{{/if}}) => {
  return (
    <Container>
      <h1><FormattedMessage defaultMessage="{{ pascalCase name }} component" id="{{ pascalCase name }} / message" /></h1>
    </Container>
  );
};
