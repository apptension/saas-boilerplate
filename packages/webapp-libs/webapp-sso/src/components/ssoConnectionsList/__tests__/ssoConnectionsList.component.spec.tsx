import { SSOConnectionsList } from '../ssoConnectionsList.component';

describe('SSOConnectionsList', () => {
  it('should be defined', () => {
    expect(SSOConnectionsList).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof SSOConnectionsList).toBe('function');
  });
});
