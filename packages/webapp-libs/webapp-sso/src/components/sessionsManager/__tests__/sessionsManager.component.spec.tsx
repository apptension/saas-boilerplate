import { SessionsManager } from '../sessionsManager.component';

describe('SessionsManager', () => {
  it('should be defined', () => {
    expect(SessionsManager).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof SessionsManager).toBe('function');
  });
});
