import { PasskeysManager } from '../passkeysManager.component';

describe('PasskeysManager', () => {
  it('should be defined', () => {
    expect(PasskeysManager).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof PasskeysManager).toBe('function');
  });
});
