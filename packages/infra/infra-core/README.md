# internal-infra-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build internal-infra-core` to build the library.

## Running unit tests

Run `nx test internal-infra-core` to execute the unit tests via [Jest](https://jestjs.io).
