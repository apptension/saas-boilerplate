---
title: Version matrix
---

import useBaseUrl from "@docusaurus/useBaseUrl";

This tool displays a list of all environment stages (e.g. `dev`, `stage`, `prod`) and their metadata:

- Currently deployed version
- Deployment time
- List of services deployed to the environment stage
- Links to services available through HTTPS

<img
  src={useBaseUrl("img/version-matrix.png")}
  alt="Version matrix"
  height="300"
/>

## Configuration

You can configure version matrix in `.awsboilerplate.json`.

##### `toolsConfig.domains.versionMatrix`

A domain used to host version matrix tool.

Type: `string`

Example: `status.awsb.apptoku.com`

## Deployment

```shell
make deploy-global-tools
```
