query allDemoItems {
  demoItemCollection {
    items {
      sys {
        id
      }
      title
    }
  }
}

query demoItem($itemId: String!) {
  demoItem(id: $itemId) {
    title
    description
    image {
      title
      url
    }
  }
}
