query allDemoItems {
  demoItemCollection {
    items {
      sys {
        id
      }
      title
      image {
        title
        url
      }
    }
  }
}

query demoItem($itemId: String!) {
  demoItem(id: $itemId) {
    title
    description
    image {
      title
      url
    }
  }
}
