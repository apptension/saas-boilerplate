import * as configSelectors from './config.selectors';
import * as configActions from './config.actions';

export { INITIAL_STATE as CONFIG_INITIAL_STATE } from './config.reducer';
export { configActions, configSelectors };
