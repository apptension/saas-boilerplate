import { createSelector } from '@reduxjs/toolkit';
import { GlobalState } from '../../config/reducers';

export const select{{ pascalCase name }}Domain = (state: GlobalState) => state.{{ camelCase name }};

export const select{{ pascalCase name }}List = createSelector(select{{ pascalCase name }}Domain, (state) => state.items);
