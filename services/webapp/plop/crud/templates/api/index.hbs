import { client } from '../client';
import {
  {{ pascalCase name }}ApiGetResponseData,
  {{ pascalCase name }}ApiPostRequestData,
  {{ pascalCase name }}ApiPostResponseData,
  {{ pascalCase name }}ApiPutRequestData,
} from './types';

export const {{ constantCase name }}_URL = '{{ apiUrl }}';

export const list = async () => {
  const res = await client.get<{{ pascalCase name }}ApiGetResponseData[]>({{ constantCase name }}_URL);
  return res.data;
};

export const get = async (id: string) => {
  const res = await client.get<{{ pascalCase name }}ApiGetResponseData>({{ constantCase name }}_URL + id + '/');
  return res.data;
};

export const add = async (data: {{ pascalCase name }}ApiPostRequestData) => {
  const res = await client.post<{{ pascalCase name }}ApiPostResponseData>({{ constantCase name }}_URL, data);
  return res.data;
};

export const update = async (data: {{ pascalCase name }}ApiPutRequestData) => {
  const res = await client.put<{{ pascalCase name }}ApiPostResponseData>({{ constantCase name }}_URL + data.id + '/', data);
  return res.data;
};

export const remove = async (id: string) => {
  const res = await client.delete({{ constantCase name }}_URL + id + '/');
  return res.data;
};
